'use strict';

app.controller('navigationController', function($scope) {
    //$scope.navigation = {};
    
    /*$scope.navigation.navigate = function(id) {
        $scope.showScreenID = id;
        console.log($scope.showScreenID);
    }*/
});

/*app.directive('animateCarousel', function($animate, $window) {
    return function (scope, element, attrs) {
        scope.$watch(attrs.pushSection, function (sectionIndex) {
            
            var push = jQuery($window).height();
            
            jQuery(element)
                .animate({
                    'top': push + 'px'
                });
        });
    }
}*/